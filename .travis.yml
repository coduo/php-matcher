language: php

sudo: false

cache:
  directories:
    - $HOME/.composer/cache

matrix:
  include:
    - php: 7.0
    - php: 7.1
    - php: hhvm

before_install:
  - composer self-update

install:
  - export COMPOSER_ROOT_VERSION=dev-master
  - composer update;

script:
  - ./bin/phpunit
